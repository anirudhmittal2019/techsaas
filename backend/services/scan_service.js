// /services/scan_service.js
const scanInvoicePhoto = async (photoPath) => {
    // AI logic for scanning invoice and extracting fields
    // Placeholder for now:
    return {
        paymentTo: 'Vendor ABC',
        paymentFrom: 'Company XYZ',
        amount: 1000,
        date: '2024-06-10'
    };
};

module.exports = { scanInvoicePhoto };
